# 9.0.4
Generate `CommandHandlerRegistration` into the `<RootNamespace>.AutoGenerated` namespace instead of the root namespace.
Retarget the sample projects to `net10.0`.
Upgrade `NetMQ` to 4.0.4.3, which targets `net10.0` in place of `net8.0`.  The public API is unchanged.
Upgrade `Albatross.Config` to 8.0.1 and `Microsoft.Extensions.Hosting.Abstractions` to 10.0.12.
# 7.7.3
Replace reference to `Albatross.Serialization` with `Albatross.Serialization.Json`.
# 7.7.0
Add messageId as the first parameter when constructing a queue name.